# React State

Adding local state to manage the Notifications drawer visibility in a React class component.

## Task 0
- Added `displayDrawer` state to `App` with `handleDisplayDrawer` and `handleHideDrawer` handlers
- Wired handlers to Notifications menu item and close button
- Updated `shouldComponentUpdate` to rerender on `displayDrawer` changes

## Run Tests
```bash
cd task_0/dashboard
npm test -- --runInBand
```
